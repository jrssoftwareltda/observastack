global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: observastack-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["observastack-api:8080"]
